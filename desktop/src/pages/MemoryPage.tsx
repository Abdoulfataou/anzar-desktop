/**
 * Page Mémoire - Historique des conversations et mémoire persistante
 */
import MemoryView from '@/components/memory/MemoryView';

export default function MemoryPage() {
  return <MemoryView />;
}
