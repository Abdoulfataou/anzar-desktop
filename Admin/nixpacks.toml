[phases.setup]
nixPkgs = ["nodejs_latest", "npm-10_x"]

[phases.install]
cmds = ["rm -f package-lock.json && npm install --include=dev"]

[phases.build]
cmds = ["npm run build:web"]

[start]
cmd = "npx serve dist -s -l $PORT"
